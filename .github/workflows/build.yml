name: Build apps

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: ${{ matrix.label }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-14
            label: macOS-AppleSilicon
            artifact: EditMyRaw-macOS-AppleSilicon.zip
          - os: macos-13
            label: macOS-Intel
            artifact: EditMyRaw-macOS-Intel.zip
          - os: windows-latest
            label: Windows
            artifact: EditMyRaw-Windows.zip
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install app + PyInstaller
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install . pyinstaller

      - name: Build with PyInstaller
        shell: bash
        run: |
          pyinstaller --noconfirm --clean --windowed --name EditMyRaw \
            --osx-bundle-identifier com.jyharju.editmyraw \
            --collect-all cv2 \
            --collect-all rawpy \
            --collect-all google \
            --collect-submodules google.genai \
            --collect-submodules tkinter \
            --collect-data certifi \
            --collect-data editmyraw \
            app_main.py

      - name: Package (macOS)
        if: runner.os == 'macOS'
        run: ditto -c -k --keepParent dist/EditMyRaw.app "${{ matrix.artifact }}"

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: Compress-Archive -Path dist/EditMyRaw -DestinationPath "${{ matrix.artifact }}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifact }}
